import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.reactjs',
  appName: 'ionic-react-js',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
